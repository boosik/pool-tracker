export const awsConfig = {
    aws_table_name: "osmosis-pool-swap-data",
    aws_local_config: {
        region: "",
        endpoint: ""
    },
    aws_remote_config: {
        access_key_id: "",
        secret_access_key: "",
        region: ""
    },
}

export const targetConfig = {
    target_pools: ["803"]
}